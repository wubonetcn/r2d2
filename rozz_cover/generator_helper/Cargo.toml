[package]
name = "generator_helper"
version = "0.1.0"
edition = "2021"

[lib]
name = "generator_helper"
crate-type = ["dylib"]

[dependencies]
common = {path = "../common"}
libc = "0.2"
colored = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"